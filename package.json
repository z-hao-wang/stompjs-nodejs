{
  "name": "stompjs-nodejs",
  "version": "0.0.2",
  "description": "A nodejs wrapper for stompjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "tsc": "tsc",
    "tsc:watch": "tsc -w",
    "prettier": "node ./node_modules/prettier/bin-prettier.js --parser=typescript --single-quote --trailing-comma=all --write --print-width=120 'src/**/*.ts'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Hao Wang",
  "license": "MIT",
  "dependencies": {
    "@stomp/stompjs": "5.3.0",
    "ws": "6.2.1"
  },
  "keywords": [ "nodejs", "activeMq", "stomp", "typescript" ],
  "repository": {
    "type": "git",
    "url": "https://github.com/z-hao-wang/stompjs-nodejs.git"
  },
  "devDependencies": {
    "@types/node": "^11.13.0",
    "prettier": "1.16.4"
  }
}
